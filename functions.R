# functions are assigned to variables ----
add <- function(x, y) return(x + y)
add(10, 11)

add <- function(x, y) {
  return(x + y)
}

sum_of_squares <- function(x) {
  sum(x^2)
}

sum_of_squares(1:5)

